Stack ob boxes

You have a stack of n boxes, with widths Wi, heights Hi and depths Di. The boxes cannoy be rotated and can only be stacked on top of each other if each box in the stack is strictly larger than the box above it in width, height and depth. Implement a method to compute the height of the tallest possible stack. The height of a stack is the sum of the heights of each box.